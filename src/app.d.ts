declare global {
  type Point = [number, number];

  namespace App {
    // interface Locals {}
    // interface PageData {}
    // interface Error {}
    // interface PageState {}
    // interface Platform {}
  }
}

export { };
